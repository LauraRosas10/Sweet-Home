import Categoria from '../schemas/categoriasSch.js';

// Exportar directamente el modelo de Mongoose para mantener compatibilidad
// con los controladores que crean instancias y usan métodos como find()
export default Categoria;
